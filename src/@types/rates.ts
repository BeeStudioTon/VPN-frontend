export interface RatesType {
    active: number
    count_days: number
    desc_rate: string
    id: number
    name_rate: string
    price: number
    price_m: number // actual price
    price_old: number
}
